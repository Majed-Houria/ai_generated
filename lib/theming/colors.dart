import 'package:flutter/material.dart';

class ColorsManager {
  // --- CYBER TEAL THEME COLORS ---
  static const Color bgStart = Color(0xFF0F1C2E); // Background Deep Blue
  static const Color bgEnd = Color(0xFF09121F);   // Background Darker Gradient
  static const Color primary = Color(0xFF14B8A6); // Teal Primary
  static const Color accent = Color(0xFF22D3EE);  // Neon Cyan Accent
  static const Color highlight = Color(0xFF6366F1); // Indigo Highlight
  static const Color success = Color(0xFF10B981); // Emerald Success
  static const Color warning = Color(0xFFEF4444); // Warning / AI Red
  static const Color textPrimary = Color(0xFFF8FAFC); // White/Slate
  static const Color textSecondary = Color(0xFF94A3B8); // Dimmed Slate
  static const Color cardBg = Color(0xFF162539);  // Foreground container
  static const Color borderDark = Color(0xFF1E334D);

  static const Color surface = Color(0xFF0F1C2E); // Deep Blue Background

  // 🔴 Error
  static const Color error = Color(0xFFC62828);
  static const Color onError = Colors.white;

  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color white60 = Colors.white60;
  static const Color black54 = Colors.black54;
}

